import React from "react";
export declare enum Severity {
    error = "error",
    warning = "warning",
    info = "info",
    success = "success"
}
export declare const severity: Severity[];
interface ISnacksProps {
    open: boolean;
    setOpen: React.Dispatch<React.SetStateAction<boolean>>;
    message: string;
    level: Severity;
}
export declare const Snacks: ({ open, setOpen, message, level }: ISnacksProps) => JSX.Element;
export {};
