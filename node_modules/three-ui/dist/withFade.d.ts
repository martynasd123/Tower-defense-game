import React, { ComponentType } from "react";
export declare const withFade: (enterTimeout: number, exitTimeout: number) => <T extends object>(WrappedComponent: React.ComponentType<T>) => React.FC<T>;
